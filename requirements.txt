# App 執行時相依（Streamlit Cloud 部署用，維持精簡以加快建置）
# app.py → predict → train → features → data_loader 這條鏈只需要以下套件：
streamlit>=1.30
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
# 開發/評估/資料下載額外工具見 requirements-dev.txt
